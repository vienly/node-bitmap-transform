#! /usr/bin/env node

'use strict';

const fs = require('fs');
const transformer = require('../lib/bitmapTransformer.js');

fs.readFile('./test/resources/palette-bitmap.bmp', function(err, data) {
  if(err) throw err;
  transformer(data);
});
